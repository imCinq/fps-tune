# Privacy

CoreTune does not collect or transmit user data.

It reads only `config/coretune.properties` and the number of particles admitted during the current client tick. It stores only the local configuration file.

There is no telemetry, analytics, advertising, account system, update checker, remote configuration, or custom network connection.
